from setuptools import setup, find_packages

setup(
    name='real2sim',
    version='0.0.1',
    author='Xuanlin Li',
    packages=find_packages(include=['real2sim*']),
    python_requires=">=3.9",
)