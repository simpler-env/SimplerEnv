from setuptools import find_packages, setup

setup(
    name="realsimple",
    version="0.0.1",
    author="Xuanlin Li",
    packages=find_packages(include=["realsimple*"]),
    python_requires=">=3.10",
)
